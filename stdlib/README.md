# Sing Stdlib Alpha

The stdlib keeps the language north star visible: short names, explicit effects, and declarations that tools can parse without prose.

These modules are alpha contracts. The interpreter currently implements `out`, `sqrt`, and `sum`; the remaining declarations pin the token-minimal API shape for upcoming runtime/backend work.
